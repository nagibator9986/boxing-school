# Живое тестирование бота

## Обновление 12.09.2026, вечер — «2» в ответ на список больше не уводит к администратору

**Что было на вашем скриншоте.** Бот предложил секции списком и попросил
написать цифру, а цифру «2» сам не понял: переспросил и передал запись
администратору.

**Что изменилось:**
- на любой список бота можно ответить цифрой — бот понимает, какой вариант
  выбран, и записывает на него;
- если в том же сообщении ушло расписание, варианты больше не пропадают из
  вопроса;
- если во время записи бот сорвался сам, он не передаёт разговор
  администратору, а задаёт следующий вопрос: фамилию и имя, возраст, секцию или
  время, с вариантами под цифрами.

Итог прогона: 75 сообщений, четыре записи, ни одной передачи администратору посреди
записи. Диалог с вашего скриншота теперь заканчивается записью: на «2» после списка
секций бот ответил «Мы записали вас на бесплатное пробное занятие! … Бокс, в субботу,
12.09 в 19:00».

---

## Обновление 12.09.2026 — запись до конца, заявки вам в WhatsApp, чаты с рекламы

**Почему бот молчал в чатах с рекламы.** Приветствие «Рады приветствовать вас в
Ainazarov Top Team!» WhatsApp отправляет сам, от имени аккаунта школы. Бот видел
сообщение из аккаунта, которое писал не он, решал, что клиенту уже отвечает
человек, и замолкал. Ваши тексты автоответов из CRM, по которым он мог бы это
понять, в WhatsApp не применялись вовсе. Теперь применяются, а приветствие, где
названа школа, бот узнаёт и без настройки.

**Почему запись обрывалась на «Да».** Бот написал «по понедельникам, средам и
пятницам», а на «Да» повторил «в понедельник». Проверка на выдуманные факты не
нашла этот день в данных хода и сняла ответ — отсюда «ответит администратор».
Кроме того, бот ни разу не спросил возраст. Теперь день, который бот уже
называл, не считается выдумкой, а недостающее (фамилия и имя, возраст, секция,
время) бот спрашивает по одному вопросу.

**Заявки на номер администратора.** В CRM → Настройки есть поле «Номер для
заявок». Туда приходят карточки записей и вопросов, на которые нужен человек.
Одна карточка на одну запись, при переносе — «ИЗМЕНЕНИЕ ЗАПИСИ». В карточке
номер клиента и ссылка на переписку в CRM. Если WhatsApp не примет карточку, бот
всё равно продолжит отвечать родителям, а заявка останется в CRM.

**Что сделать вам:**
1. Передеплой на Railway.
2. Вписать номер администратора в «Номер для заявок» и один раз написать с этого
   номера на номер бота.
3. В CRM открыть замолчавшие чаты с рекламы и нажать «Вернуть бота в диалог».

Итог прогона: 68 ходов, три записи. Цепочка с вашего скриншота дошла до записи
на понедельник, 14.09 в 19:00, без передачи администратору. Чат с рекламы с
новым приветствием получил ответ.

Два ответа сняла проверка на выдуманные цифры. В обоих случаях бот назвал цену
(«2 083 ₸») или время («в 17:30»), не сверив их в этом же сообщении, и вопрос
ушёл администратору. Во втором диалоге следующим сообщением запись всё равно
закрылась. Это отдельные правки.

---

## Обновление 10.09.2026, ночь — бот сам записывает на пробное

**Запись до конца, без «администратор свяжется».** Родитель выбрал зал, время и
секцию — бот отвечает так:

```
✅ Мы записали вас на бесплатное пробное занятие!

👤 Иванов Али
🥊 Бокс
📍 КСК — школа №9, Каирбекова 334, школа №9 (цокольный этаж)
🗓 Во вторник, 15.09 в 17:00

Приходите за 10 минут до начала, чтобы ребёнок успел переодеться, и обязательно подойдите к тренеру.
```

Ниже в том же сообщении — что взять с собой, вашим текстом.

**Время, секцию, фамилию, имя и возраст бот берёт только из слов родителя.** В
прогоне модель сама «записала» ребёнка на субботу 17:00, которую никто не
выбирал. Теперь, если родитель чего-то не назвал, бот спрашивает. Если родитель
назвал время, которого нет в расписании («17:30»), бот говорит, что такого
занятия нет, и предлагает варианты.

**Фамилия и имя.** Бот спрашивает: «Запишу на бесплатное пробное — подскажите
фамилию, имя и возраст ребёнка?». Если родитель не хочет называть фамилию,
бот записывает по имени.

**После видео и расписания** отдельным сообщением идёт «Записать ребёнка на
первую пробную тренировку?». Сообщения приходят строго по порядку: список залов,
потом вопрос; расписание, видео, потом предложение записи.

**Тобыл — по городским ценам** (25 000 / 30 000 ₸). Вопрос из прошлого
обновления закрыт.

**Остальное по вашим сообщениям:**
- КСК: добавлены Вт·Чт·Сб 17:00–18:30, бокс и кикбоксинг.
- Гибкий абонемент: перерасчёт, если заранее предупредить о пропуске, при
  болезни — по справке. За 25 000 перерасчёта нет.
- Возраст: «Принимаем детей с 5 лет — и мальчиков, и девочек».
- Сообщение «Вчера вас не было на пробном» отключено.

**Что ещё нашёл прогон и я починил:**
- бот больше не зовёт администратора, когда повторяет время из расписания,
  которое сам прислал ходом раньше;
- к списку залов, который сам спрашивает «Какой из них вам ближе?», больше не
  дописывается второй вопрос — ни от воронки, ни от модели;
- бот больше не снимает свой ответ за фразу, дословно совпавшую с примером из
  его инструкции;
- из инструкции убрано конкретное время: модель брала «17:30» из примера и
  называла его клиенту;
- ответ, снятый проверкой, больше не остаётся в памяти модели, и она не
  повторяет ошибку на следующих ходах;
- если бот всё же передаёт вопрос администратору, он не шлёт городскому
  клиенту прайс райцентров и не повторяет прайс, который клиент уже видел.

Итог прогона: 59 ходов, ни одного ответа, снятого проверкой, и ни одного
сообщения без вопроса. Сценарий 21 дошёл до записи на пятницу, 17:00, после
того как бот сказал, что в 17:30 занятия нет.

Честно о сбоях: пока шёл прогон, компьютер засыпал, и в сценариях 16, 18 и 22
модель не ответила вовремя. Бот передал эти вопросы администратору, а в
сценарии 22 следующее сообщение клиента осталось без ответа — после сбоя бот
15 минут молчит. Это не ошибка логики, но поведение после сбоя стоит обсудить
отдельно. В предыдущем прогоне, без сбоев, запись в сценарии 22 тоже закрылась.

Одно наблюдение: в сценарии 06 на «А где именно на КСК?» модель прислала список
залов области вместо адреса. Слежу за этим.

---

## Обновление 10.09.2026, вечер — возраст и шаг к записи

**Возраст в карточке расписания.** Было «возраст группы уточнит администратор»,
стало «Принимаем детей с 5 лет — отдельных возрастных групп нет». Число берётся
из ответа базы знаний про возраст, а не зашито в код: поправите ответ — карточка
изменится сама.

**После расписания бот предлагает запись**, а не просто спрашивает про время:
«Хотите записать ребёнка на бесплатное пробное?».

**Седьмой пункт** — «Западный микрорайон — БЦ «Кеме»», адрес «Полевая 7/3,
напротив 5-й поликлиники», без повтора бизнес-центра. **Тобыл** в списке просто
восьмым, без пометки про цены.

**Один вопрос к вам.** Пометку «цены ниже» я убрал, но в базе для Тобыла и
райцентров стоит абонемент **10 000 ₸** против **25 000 ₸** в городе — и бот
называет клиентам из Тобыла 10 000 ₸. Если цена везде одна, это правка не
текста, а самих цен: подтвердите, и я поправлю прайс.

Итог прогона: 44 хода из 44 ведут клиента дальше, ни одного снятого ответа.

---

## Обновление 10.09.2026 — восьмой зал, Западный микрорайон, выбор номером

**Почему бот показывал семь залов.** Список по городу собирается только из
залов с признаком «город», а Тобыл заведён как область — вместе с Карабалыком,
Фёдоровкой и остальными райцентрами. Он попадал в общую строку «работаем ещё в
7 населённых пунктах», но по имени не назывался. Разделение сделано не просто
так: абонемент в области стоит 10 000 ₸ против 25 000 ₸ в городе, и смешивать
их в один список нельзя — клиент решит, что цена общая.

Теперь Тобыл стоит в списке города **восьмым**, с пометкой «пригород, цены ниже
городских». Остальные райцентры остались отдельной строкой, теперь их шесть.
Признак настраивается в базе (`list_with_city`), так что при желании в список
можно добавить и другой пригород.

**Полевая переименована.** Было «Полевая — БЦ «Кеме»», стало **«Западный
микрорайон — БЦ «Кеме»»**, адрес — «Полевая 7/3, бизнес-центр «Кеме»»,
ориентир — «напротив 5-й поликлиники». Бот теперь узнаёт этот зал по словам
«западный», «западный микрорайон», «поликлиника», «батыс».

**Номер зала наконец работает.** Карточка звала «напишите номер зала», а на «7»
бот отвечал «в семь лет ребёнок уже отлично понимает тренера», на «8» —
«как зовут вашего ребёнка?». Модель принимала номер за возраст. Теперь номер
разбирает код — как цифру в меню: «7» присылает расписание Западного
микрорайона, «8» — Тобыла. Цифра вне списка и цифра без карточки залов
по-прежнему остаются возрастом.

**Цена, которую бот сам прислал, больше не считается выдумкой.** Прайс
отправляет код, а на следующем ходу модель на него ссылалась — и фильтр снимал
ответ, потому что в том ходу инструмент не вызывался. Клиент получал «ответит
администратор» на вопрос, ответ на который уже был у него в чате.

**«А если двое детей?»** тоже опознаётся как вопрос о деньгах: в прайсе есть
семейная скидка.

Итог прогона: **44 хода из 44 ведут клиента дальше**, снятых ответов — один
(в нём бот честно передал вопрос администратору).

---

## Обновление 09.09.2026 — меню, воронка и утреннее расписание

**Меню теперь из трёх пунктов.** Ветку «мы уже занимаемся» владелец убрал: под
бота открыт отдельный номер, и действующие клиенты пишут не сюда. Осталось:
записаться на пробное, узнать цены и залы, написать менеджеру.

**Пункт «цены, залы и расписание» больше не может сорваться.** Владелец увидел
на него ответ «здесь лучше ответит администратор» и спросил: «Зачем тогда 2
пункт?». Причина была в том, что ответ собирала модель: в одном прогоне из
десяти она называла цену словами, без вызова инструмента, и фильтр справедливо
снимал ответ — а клиент получал отказ на пункт, который бот сам ему предложил.
Теперь список залов и прайс собирает код: сорваться нечему, отвечает пункт
мгновенно и всегда одинаково.

**Бот ведёт клиента.** Каждый ответ заканчивается одним вопросом — следующим
шагом воронки: возраст → район → время → имя → номер → запись. Правило записано
в промпт, но не оставлено на усмотрение модели: если она закончила ответ фактом,
вопрос дописывает код, по тому, чего ещё не хватает для записи. Вопрос не
добавляется там, где он неуместен: после прощания, при передаче администратору и
в разговоре с ребёнком.

В прогоне 20 диалогов **все 44 хода ведут клиента дальше** (было 43 из 44), и ни
один ответ не снят фильтром (было 1–2 за прогон).

**Возражение по цене — тоже вопрос о деньгах.** На «дорого, есть подешевле?» бот
звал администратора, потому что таких слов не было в словаре. Теперь «дорого»,
«подешевле», «скидки», «рассрочка», «қымбат» ведут к прайсу, где есть и скидка
на второго ребёнка, и разовая тренировка.

**Расписание от Зарины добавлено** — утренние и дневные группы:

| Зал | Добавлено |
|---|---|
| Северо-Западный 70, участок 51 | Пн·Ср·Пт 09:00–10:30, 17:00–18:30, 19:00–20:30; Вт·Чт·Сб 09:00–10:30 |
| Каирбекова 24 | Пн·Ср·Пт 09:00–10:30 |
| Воинов-интернационалистов 8Б | Вт·Чт·Сб 09:00–10:30 |

Утренние группы помечены как подходящие для второй смены: кто учится во вторую,
увидит их первыми, а кто в первую — вечерние. **Проверьте два допущения:** утром
идут обе секции (бокс и кикбоксинг), как и во всех остальных залах базы, и
дневная группа 17:00–18:30 открыта для любой смены.

---

## Прогон 03.09.2026

## Что это

Двадцать сквозных диалогов, прогнанных через **боевой** код: вебхук Wazzup →
защита → модель Gemini с настоящим ключом → инструменты (расписание, цены,
видео, заявки) → анти-галлюцинационный фильтр → очередь исходящих → воркер
отправки. Заглушка одна: сам HTTP-вызов в Wazzup, потому что кабинет школы ещё
не заведён и писать физически некуда. Всё остальное — то же, что в бою.

Диалоги целиком: [dialogs.md](dialogs.md). Повторить: `python3 scripts/live_dialogs.py`.

## Что нашли и починили

Прогонов было четыре: каждый следующий — после починки предыдущих находок.

**1. Бот замолкал на час после того, как сам передавал вопрос человеку.**
Самое дорогое. Передав вопрос администратору, бот ставил себе паузу на 60 минут
— и молчал на всё, что клиент писал дальше. В прогоне это дважды оставило
клиента без ответа: «а справка нужна какая-то?» и «давайте тогда в субботу на
пробную придём» ушли в тишину. Теперь пауза, которую бот поставил себе сам,
снимается новым вопросом клиента — при условии, что человек в диалог не входил.
Как только человек напишет, диалог замолкает по другой, более сильной причине,
и её сообщение клиента не снимает.

**2. То же самое после срабатывания фильтра.** Когда фильтр снимал ответ бота
(защита от выдумок), диалог тоже вставал на час. В прогоне на этом потерялась
самая ценная реплика: «Асель, 87015551122» — имя и телефон на точке записи.
Теперь клиент за ошибку бота молчанием не платит.

**3. Бот дважды подряд спрашивал имя ребёнка.** Клиент отвечал «Асель,
87015551122», а в ответ получал «как зовут сына?». Телефон и возраст из реплики
разбирались, имя — нет. Теперь разбирается и имя: город в падеже («Тобыле»),
местоимение («ему 8 лет») и длинный рассказ за имя не принимаются.

**4. Ребёнку продолжали продавать.** Девятилетний написал сам, бот верно
попросил позвать родителей — а на следующей реплике «родители не знают пока»
забыл, с кем говорит, и снова начал собирать данные для записи. Признак «пишет
ребёнок» теперь держится весь разговор и снимается только приходом взрослого
(«это мама», «мой сын»).

**5. Верный ответ снимался из-за названия посёлка.** На «мы в Тобыле, сколько
стоит?» бот считал цену правильно, но ответ не уходил: проверка названий
сверялась только с данными текущего хода, а посёлок вернул не инструмент, а
клиент. Теперь место из базы знаний выдумкой не считается. При этом «зал в
Затобольске» по-прежнему блокируется: район знаком, но зала там нет.

**6. «Филиал в Алматы» проходил насквозь.** Проверка не видела слова с заглавной
буквы в начале предложения. Теперь видит.

## Что осталось на ваше решение

- **«Дорого, есть подешевле?»** — бот передаёт вопрос администратору вместо
  того, чтобы назвать скидки (10% на второго ребёнка, 15% на третьего) и разовое
  занятие. Ответ честный, но слабый для продажи. Скажите, что тут отвечать, — и
  я закреплю это в базе.
- **Способы оплаты** и **тренеры** — бот честно передаёт человеку, потому что
  данных нет. Ждём от вас.
- **Автоприветствие рекламы** распознаётся по тексту («ЖМИ ОТПРАВИТЬ», «Thank
  you for contacting us»). Поменяете текст в WhatsApp Business — впишите новый в
  настройках админки, иначе бот примет его за оператора и замолчит.

## Как дать правки

Читайте `dialogs.md` подряд и пишите замечания прямо по номерам сценариев —
например: «04, третья реплика: не спрашивать возраст, он уже был». Я разберу по
списку.
